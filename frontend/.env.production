# Produção Docker — API via proxy nginx /api (mesmo origin, sem CSP issues)
# Anular as variáveis do .env base para forçar o fallback relativo '/api'
VITE_API_BASE_URL=
VITE_API_URL=
VITE_GRAFANA_URL=
